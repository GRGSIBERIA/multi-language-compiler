#include "stdafx.h"
#include "Python.h"
using namespace mlc::lang;

Python::Python()
{
	/*
	class Vertex:
		def __init__(self):
			self.v = Vector3()

		def read(self, bytearray, index):
			# 
			# x, y, z
			self.v = Vector3(bytearray, index)

		def write(self, bytearray):
			self.v.write(bytearray)

	class Vector3:
		def __init__(self):
			self.x = 0
			self.y = 0
			self.z = 0

		def read(self, bin, index):
			self.x, self.y, self.z = struct.unpack_from(">fff", bin, index)

		def write(self, bin):
			bin.write(struct.pack(">fff", self.x, self.y, self.z))
	*/
}


Python::~Python()
{
}
